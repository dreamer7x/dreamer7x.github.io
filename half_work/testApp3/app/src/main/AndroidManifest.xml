<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testapp3">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".MoreReplyActivity"></activity>
        <activity android:name=".DiscussActivity" />
        <activity android:name=".SendTrendActivity" />
        <activity android:name=".SetActivity" />
        <activity android:name=".AttentionsFansActivity" />
        <activity android:name=".TrendActivity" />
        <activity android:name=".MineTrendsActivity" />
        <activity android:name=".OtherTrendsActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".ClockInActivity" />
        <activity android:name=".MainWindowsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>